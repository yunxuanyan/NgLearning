import { ColourChangerDirective } from './colour-changer.directive';

describe('ColourChangerDirective', () => {
  it('should create an instance', () => {
    const directive = new ColourChangerDirective();
    expect(directive).toBeTruthy();
  });
});
