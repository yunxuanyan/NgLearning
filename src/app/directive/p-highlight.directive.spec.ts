import { PHighlightDirective } from './p-highlight.directive';

describe('PHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new PHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
